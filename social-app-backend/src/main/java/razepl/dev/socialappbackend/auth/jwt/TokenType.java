package razepl.dev.socialappbackend.auth.jwt;

/**
 * An enumeration representing types of tokens in application.
 * Available types is JWT_BEARER_TOKEN.
 */
public enum TokenType {
    JWT_BEARER_TOKEN
}
