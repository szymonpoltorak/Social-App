/**
 * The interface for the SearchSite.
 * Provides a method to load data for the site.
 */
export interface SearchSiteInterface {

    /**
     * Loads data for the site.
     * This method does not return any value.
     */
    loadData(): void;
}
