/**
 * Represents the interface for the Edit Dialog.
 */
export interface EditDialogInterface {
    /**
     * Exits the edit dialog.
     */
    exit(): void;

    /**
     * Submits the edited value.
     */
    submitValue(): void;
}
